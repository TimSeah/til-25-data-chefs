# FastAPI and Uvicorn
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5 # For file uploads in FastAPI

# OCR Engine
easyocr>=1.7.0 # Or latest version

# Image processing and numerical operations
Pillow>=9.0.0
numpy>=1.20.0,<2.0 # Pinning numpy to <2.0
opencv-python-headless>=4.5.0 # EasyOCR might use OpenCV components or it's useful for preprocessing

# Utilities
PyYAML>=5.4 # If you still use YAML for other configs